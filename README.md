## How to compile this project ?

```bash
mkdir bin
```

compile the files

```bash
javac -d bin src/*.java
```

run the project

```bash
java -cp bin src.App
```

all in one

```bash
javac -d bin src/*.java && java -cp bin src.App
```
